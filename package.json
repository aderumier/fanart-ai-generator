{
  "name": "geminibatch",
  "version": "1.0.0",
  "description": "Batch-process a folder of images through the Gemini web chat (browser automation, no API) and save the generated fanart.",
  "type": "module",
  "scripts": {
    "chrome": "bash start-chrome.sh",
    "start": "node index.js",
    "probe": "node probe.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "sharp": "^0.33.5"
  }
}
