{
  "name": "geminibatch",
  "version": "1.2.0",
  "description": "Batch-process images through the Gemini web chat (browser automation, no API) and save the generated fanart.",
  "type": "module",
  "scripts": {
    "chrome": "bash start-chrome.sh",
    "start": "node index.js",
    "probe": "node probe.js"
  },
  "dependencies": {
    "@pilio/gemini-watermark-remover": "^1.0.21",
    "chromium-bidi": "^16.0.1",
    "jimp": "^0.22.12",
    "playwright-core": "^1.49.0"
  }
}
